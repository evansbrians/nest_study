[Unit]
Description=Run the nest_study nightly batch load once a day

[Timer]
# 02:30 local time, with a little jitter; catch up if the VM was asleep.
OnCalendar=*-*-* 02:30:00
RandomizedDelaySec=300
Persistent=true

[Install]
WantedBy=timers.target
